---
context: default
phase: n/a (backlog-driven, no GSD phase)
task: 12 of 12 done, 5 V2 items remaining
status: paused
last_updated: 2026-09-24
---

# Forex Trading Decision Support — Session Handoff

## Current State

MVP functionally complete (PRD 29 criteria 1–10) plus V2 foundations.
Backend 119/119 tests OK · `npm run build` clean · Neon Postgres 21 tables
(migrations `001`, `002_v2_news_events`, `003_seed_sessions` applied via idempotent `scripts/migrate.py`).
`.env` present locally (DATABASE_URL + generated SECRET_KEY, git-ignored).

Single source of truth for decisions: `backend/app/services/decision/evaluate.py`
(`evaluate()` → used by signals/latest, scanner, analysis, alerts/evaluate, persistence).

## Completed This Session

1. **AI Analyst** — `services/ai/analyst.py` (deterministic template, guardrail `self_check`), `GET /api/v1/ai/explain`, frontend panel, 6 tests.
2. **DB live** — SECRET_KEY generated into `.env`; `migrate.py` now idempotent (tracking table + baseline).
3. **ENTER reachable** — `services/analysis/price_action.py` (engulfing/pin, spec `CALCULATIONS.md §17`); `evidence.price_action` on signal.
4. **Alerts in-app** — `services/alerts/` (file store, one-shot), CRUD + evaluate endpoints, `/alerts` page.
5. **Unification** — scanner + signals/latest + analysis all project from `evaluate()`; anti-divergence test.
6. **MTF** — 5-TF biases + alignment in every output; frontend panel; analyst `mtf_note`.
7. **Persistence** — `services/decision/persist.py`: market_analyses → setups → setup_confirmations → signals (immutable snapshots) → decision_audit_logs; `POST/GET /api/v1/signals` (login); strategy bootstrap idempotent; frontend Record + history.
8. **Conditions** — location (support/live-FVG, spec §14.5), volatility (BB-width 1.5× guard, §8.1), spread (`NOT_APPLICABLE` without feed, §14.6).
9. **Risk filters** — `services/risk/limits.py` (6 rules, SKIP-when-unmeasured, §10.1); endpoint extended backward-compatibly; engine `risk` uses real plan (float tolerance 1e-9 kept).
10. **Real entry zones** — `services/analysis/supply_demand.py` (displacement + BOS + OB + mitigation); `trade_plan()` prefers newest live zone, labeled `atr_proxy` fallback; containment beats proximity (§14.5).
11. **Lifecycle** — `POST /api/v1/signals/{id}/status` enforces `DATABASE.md §38` (owner-only); frontend action buttons.
12. **MTF gate + news + scanner filters + journal link** — weak/invalid MTF → NO_TRADE (§16.1); `economic_events` + blackout rule (§18) blocking HIGH; scanner pair/strategy/session filters (+ seeded `market_sessions`); paper orders accept `signal_id` → flows into journal `decision_snapshot`.

## Decisions Made

- Template analyst, no LLM (cost + determinism); LLM seam for V3.
- File store for alerts; PG tables reserved for V2 (matches strategies/paper precedent).
- Explicit login POST for recording; never write-on-GET.
- Missing measurements → NOT_READY / OFF / SKIP, never assumed-safe.
- MTF gate downgrades only; ATR-proxy fallback labeled, never fabricated.

## Remaining (V2+)

- `risk_checks` writes (needs `risk_profiles` UI first).
- `journal_entries` PG table (currently file-store via paper broker).
- Licensed calendar source (replaces manual event input); CSV event import.
- WebSocket realtime (currently polling).
- Broker integration V4 (must stay isolated from analytical agents).

## Required Reading (in order)

1. `AGENTS.md` — agent boundaries; AI never overrides engine/risk.
2. `ARCHITECTURE.md` §§14–18, §37–39 — rule engine, AI cost rules, invariants.
3. `CALCULATIONS.md` §§8.1, 10.1, 14.5, 14.6, 16.1, 17, 18 — decision rules added this session.
4. `backend/app/services/decision/evaluate.py` — decision core.
5. `backend/app/services/decision/persist.py` — evidence chain + lifecycle.
6. `.env.example` — local setup (never commit `.env`).

## Critical Anti-Patterns (do NOT repeat)

- `edit` with multi-line oldString can delete instead of insert — always re-read the edited region after every edit.
- PowerShell: no `&&` (use `;`), use `npm.cmd` (npm.ps1 blocked), `ls` single path only.
- unittest piped output exits 1 spuriously — trust `OK`/`FAILED` text, not exit code.
- Postgres `CREATE INDEX IF NOT EXISTS` requires an index name; seed inserts need `WHERE NOT EXISTS` (no unique constraint to conflict on).
- R:R float comparison needs 1e-9 tolerance (2.0 computes as 1.9999999999999996).
- Simulator data is pattern/zone-free by construction — WAIT on simulator is correct, not a bug; prove ENTER with synthetic candles.

## Infrastructure State

- Neon Postgres live (DATABASE_URL in local `.env`); 21 tables; 4 pairs seeded; sessions seeded.
- No background servers running (all verification via unittest + `npm run build`).
- `__pycache__/*.pyc` are git-tracked — do NOT commit them (see next_action).

## Next Action

Start with: pick one V2 slice above with the user, implement with deterministic tests, then `python -m unittest discover -s tests` + `npm.cmd run build`.
Commit first: `git add` everything EXCEPT `__pycache__` (use `git add -A -- ':!*.pyc'`), message `wip: backlog sweep 12/12 done, 119 green`.
To resume: `/gsd-resume-work`
